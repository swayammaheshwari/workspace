# Add all of the environmental variables here instead of 
# embedding them directly in the app and utilize them 
# with the `DotEnv` package.

PORT=5000
HOST=http://localhost:
MONGOLAB_URI=mongodb://127.0.0.1/workspace

REDIS_PASSWORD=your_redis_password
REDIS_HOST=localhost
REDIS_PORT=6379

POSTGRES_USER=postgres
POSTGRES_HOST=localhost
POSTGRES_DATABASE=localData
POSTGRES_PASSWORD=admin
POSTGRES_PORT=5432

# The following are optional for local development, but required if you want to deploy your application on Heroku.
SECRET_KEY="YourSecretKeyGoesHere"
DEBUG=true