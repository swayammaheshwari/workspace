// Example: main.js

// Require the file where ExecutiveCode is exported
const { ExecutiveCode } = require("./script");

// Now you can use the ExecutiveCode variable
console.log("Executive Code:", ExecutiveCode);
